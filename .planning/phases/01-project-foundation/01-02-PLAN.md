---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/setup.ts
  - tests/teardown.ts
  - tests/smoke/server.test.ts
  - tests/smoke/shutdown.test.ts
  - tests/integration/db-connect.test.ts
  - tests/integration/migrations.test.ts
  - tests/unit/config-validation.test.ts
  - tests/unit/helmet-headers.test.ts
  - tests/unit/cors.test.ts
  - tests/unit/errors.test.ts
  - tests/unit/error-handler.test.ts
  - tests/unit/validation.test.ts
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "Every file listed in 01-VALIDATION.md Wave 0 exists before production implementation starts."
    - "Server, shutdown, Prisma, migration, config, security, error, and validation behaviors are captured as executable test contracts."
    - "Implementation plans can make the Wave 0 tests pass without inventing new behavior."
  artifacts:
    - path: "tests/smoke/shutdown.test.ts"
      provides: "Graceful shutdown contract"
    - path: "tests/unit/config-validation.test.ts"
      provides: "Startup config validation contract"
    - path: "tests/unit/error-handler.test.ts"
      provides: "Production unknown-error disclosure contract"
  key_links:
    - from: "tests/smoke/server.test.ts"
      to: "src/app.ts"
      via: "supertest import"
      pattern: "app"
    - from: "tests/smoke/shutdown.test.ts"
      to: "src/server.ts"
      via: "server lifecycle helper import"
      pattern: "shutdown"
    - from: "tests/unit/validation.test.ts"
      to: "src/routes/health.routes.ts"
      via: "POST /health/probe invalid body"
      pattern: "VALIDATION_ERROR"
---

<objective>
Create the complete Wave 0 validation contract before any implementation plan writes production source.

Purpose: Satisfy Nyquist coverage for SETUP-01 by ensuring every behavior in 01-VALIDATION.md has an executable test file up front.
Output: Shared test setup/teardown plus smoke, integration, and unit test contracts for all Phase 1 behaviors.
</objective>

## Phase Goal

**As a** task API maintainer, **I want to** start and validate a secure Express and Prisma foundation, **so that** later authentication and task slices can build on a working API stack.

<execution_context>
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/workflows/work-task-issue.md
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@.planning/phases/01-project-foundation/01-PATTERNS.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
@AGENTS.md

<interfaces>
Contracts these tests establish for later plans:
- `src/app.ts` exports `app` for supertest.
- `src/server.ts` exports start/shutdown lifecycle helpers that can be exercised without permanently exiting the test process.
- `src/models/index.ts` exports `prisma` as a PrismaClient instance.
- `src/config/index.ts` exports `loadConfig(env)` and `config`.
- Error responses use `{ error: { code: string, message: string, details?: any } }`.
</interfaces>
</context>

<must_haves>
Truths: all 01-VALIDATION.md Wave 0 files exist; tests are executable Vitest files; production source is not implemented in this plan.
Artifacts: tests/setup.ts, tests/teardown.ts, tests/smoke/server.test.ts, tests/smoke/shutdown.test.ts, tests/integration/db-connect.test.ts, tests/integration/migrations.test.ts, tests/unit/config-validation.test.ts, tests/unit/helmet-headers.test.ts, tests/unit/cors.test.ts, tests/unit/errors.test.ts, tests/unit/error-handler.test.ts, tests/unit/validation.test.ts.
Key links: tests import the planned source contracts directly; migration test uses Prisma deploy verification; shutdown test targets server lifecycle helpers rather than process-killing side effects.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Add server DB test contracts</name>
  <files>tests/setup.ts</files>
  <files>tests/teardown.ts</files>
  <files>tests/smoke/server.test.ts</files>
  <files>tests/smoke/shutdown.test.ts</files>
  <files>tests/integration/db-connect.test.ts</files>
  <read_first>tests/setup.ts</read_first>
  <read_first>tests/teardown.ts</read_first>
  <read_first>tests/smoke/server.test.ts</read_first>
  <read_first>tests/smoke/shutdown.test.ts</read_first>
  <read_first>tests/integration/db-connect.test.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only tests/setup.ts, tests/teardown.ts, tests/smoke/server.test.ts, tests/smoke/shutdown.test.ts, and tests/integration/db-connect.test.ts.
    Forbidden: src/*, prisma/*, package.json.
    Out of scope: migrations, config/security tests, error tests, and validation tests.
  </boundaries>
  <action>Create executable tests from 01-VALIDATION. tests/setup.ts must set NODE_ENV to test and provide deterministic defaults without masking an explicitly supplied DATABASE_URL. tests/teardown.ts must disconnect Prisma when src/models/index.ts exists. tests/smoke/server.test.ts must use supertest against src/app.ts and assert GET /health returns 200 with status "ok", and POST /health/probe with a unique test email returns 201 with id and email. tests/smoke/shutdown.test.ts must import the planned server lifecycle helpers from src/server.ts and assert graceful shutdown closes the HTTP server and calls prisma.$disconnect without forcing the Vitest process to exit. tests/integration/db-connect.test.ts must import prisma from src/models/index.ts and assert SELECT 1 succeeds.</action>
  <acceptance_criteria>
    - Server smoke tests reference src/app.ts as the contract later plans implement.
    - Shutdown test references src/server.ts lifecycle helpers and does not kill the test runner.
    - DB connection test references src/models/index.ts and fails until the Prisma client exists.
    - Test setup does not silently replace a user-provided DATABASE_URL.
  </acceptance_criteria>
  <verify><automated>rtk npx vitest run tests/smoke/server.test.ts tests/smoke/shutdown.test.ts tests/integration/db-connect.test.ts</automated></verify>
  <done>Server startup, graceful shutdown, HTTP DB write, and Prisma connection contracts exist before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration and security contracts</name>
  <files>tests/integration/migrations.test.ts</files>
  <files>tests/unit/config-validation.test.ts</files>
  <files>tests/unit/helmet-headers.test.ts</files>
  <files>tests/unit/cors.test.ts</files>
  <read_first>tests/integration/migrations.test.ts</read_first>
  <read_first>tests/unit/config-validation.test.ts</read_first>
  <read_first>tests/unit/helmet-headers.test.ts</read_first>
  <read_first>tests/unit/cors.test.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only tests/integration/migrations.test.ts, tests/unit/config-validation.test.ts, tests/unit/helmet-headers.test.ts, and tests/unit/cors.test.ts.
    Forbidden: src/*, prisma/*, package.json.
    Out of scope: implementation source and error/validation middleware tests.
  </boundaries>
  <action>Create the migration and security tests from 01-VALIDATION. tests/integration/migrations.test.ts must run the non-interactive migration verification command appropriate for an already-created migration, `rtk npx prisma migrate deploy`, against the configured DATABASE_URL. tests/unit/config-validation.test.ts must cover missing DATABASE_URL, missing JWT_SECRET, JWT_SECRET shorter than 64 characters, invalid PORT, and valid config parsing. tests/unit/helmet-headers.test.ts must assert GET /health includes Helmet headers such as x-dns-prefetch-control and x-frame-options. tests/unit/cors.test.ts must assert production CORS rejects an unlisted Origin and allows configured origins.</action>
  <acceptance_criteria>
    - Migration test fails when migrations have not been created or applied.
    - Config validation test names loadConfig as the pure function contract.
    - Helmet and CORS tests reference src/app.ts and keep GET /health behavior intact.
    - No production source files are changed.
  </acceptance_criteria>
  <verify><automated>rtk npx vitest run tests/integration/migrations.test.ts tests/unit/config-validation.test.ts tests/unit/helmet-headers.test.ts tests/unit/cors.test.ts</automated></verify>
  <done>Migration, startup config, Helmet, and CORS contracts exist before implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Add error validation contracts</name>
  <files>tests/unit/errors.test.ts</files>
  <files>tests/unit/error-handler.test.ts</files>
  <files>tests/unit/validation.test.ts</files>
  <read_first>tests/unit/errors.test.ts</read_first>
  <read_first>tests/unit/error-handler.test.ts</read_first>
  <read_first>tests/unit/validation.test.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only tests/unit/errors.test.ts, tests/unit/error-handler.test.ts, and tests/unit/validation.test.ts.
    Forbidden: src/*, prisma/*, package.json.
    Out of scope: implementation source and security middleware tests.
  </boundaries>
  <action>Create the error and validation tests from 01-VALIDATION. tests/unit/errors.test.ts must cover AppError serialization plus NotFoundError, ValidationError, ForbiddenError, and ConflictError status codes per D-04 and D-05. tests/unit/error-handler.test.ts must cover AppError handling and production unknown errors returning INTERNAL_ERROR without stack, details, or original message. tests/unit/validation.test.ts must cover invalid POST /health/probe returning HTTP 400 with error.code VALIDATION_ERROR, valid email still returning HTTP 201 with id and email, and validate(schema, target) supporting body, query, and params targets per D-08 through D-10.</action>
  <acceptance_criteria>
    - Error tests enforce the canonical `{ error: { code, message, details? } }` envelope.
    - Unknown production error tests prevent stack or internal message disclosure.
    - Validation tests cover invalid and valid POST /health/probe behavior.
    - No production source files are changed.
  </acceptance_criteria>
  <verify><automated>rtk npx vitest run tests/unit/errors.test.ts tests/unit/error-handler.test.ts tests/unit/validation.test.ts</automated></verify>
  <done>Canonical error and Zod validation contracts exist before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process to application contracts | Tests import source modules that later plans implement. |
| test process to database | Integration tests connect to the configured PostgreSQL database. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | tests/unit/error-handler.test.ts | mitigate | Task 3 creates production unknown-error tests for canonical INTERNAL_ERROR without stack/details. |
| T-01-02 | Information Disclosure | tests/unit/helmet-headers.test.ts | mitigate | Task 2 creates Helmet header tests before middleware implementation. |
| T-01-03 | Elevation of Privilege | tests/unit/cors.test.ts | mitigate | Task 2 creates CORS allowlist rejection/acceptance tests before middleware implementation. |
| T-01-04 | Denial of Service | tests/unit/config-validation.test.ts and tests/smoke/shutdown.test.ts | mitigate | Tasks 1 and 2 create startup validation and graceful shutdown tests. |
| T-01-05 | Tampering | tests/integration/migrations.test.ts | mitigate | Task 2 creates migration deployment verification before schema implementation. |
| T-01-06 | Information Disclosure | tests/unit/validation.test.ts | mitigate | Task 3 creates canonical validation envelope tests. |
| T-01-SC | Tampering | npm installs | mitigate | Depends on Plan 01 package legitimacy checkpoint approval. |
</threat_model>

<verification>
Run `rtk npx vitest run tests/smoke/server.test.ts tests/smoke/shutdown.test.ts tests/integration/db-connect.test.ts tests/integration/migrations.test.ts tests/unit/config-validation.test.ts tests/unit/helmet-headers.test.ts tests/unit/cors.test.ts tests/unit/errors.test.ts tests/unit/error-handler.test.ts tests/unit/validation.test.ts`; failures are expected until Plans 03 through 05 implement the referenced contracts.
</verification>

<success_criteria>
Every Wave 0 file listed in 01-VALIDATION.md exists before production implementation plans run, including tests/smoke/shutdown.test.ts.
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-02-SUMMARY.md` when done.
</output>
