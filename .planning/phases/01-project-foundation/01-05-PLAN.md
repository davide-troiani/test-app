---
phase: 01-project-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - src/app.ts
  - src/utils/errors.ts
  - src/middleware/error.middleware.ts
  - tests/unit/errors.test.ts
  - tests/unit/error-handler.test.ts
  - src/middleware/validation.middleware.ts
  - src/routes/health.schema.ts
  - src/routes/health.routes.ts
  - tests/unit/validation.test.ts
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "Application errors use the canonical JSON envelope."
    - "Unknown production errors return INTERNAL_ERROR without stack or internal details."
    - "Zod validation failures return 400 with `{ error: { code, message, details? } }`."
    - "The full Phase 1 test suite passes after migration deployment."
  artifacts:
    - path: "src/utils/errors.ts"
      provides: "AppError, NotFoundError, ValidationError, ForbiddenError, ConflictError"
    - path: "src/middleware/error.middleware.ts"
      provides: "Global Express error handler"
    - path: "src/app.ts"
      provides: "Final middleware ordering with errorHandler after routes"
    - path: "src/middleware/validation.middleware.ts"
      provides: "Zod request validation middleware"
    - path: "src/routes/health.schema.ts"
      provides: "Route-owned health probe validation schema"
    - path: "tests/unit/error-handler.test.ts"
      provides: "Production unknown-error disclosure tests"
    - path: "tests/unit/validation.test.ts"
      provides: "Canonical validation error envelope tests"
  key_links:
    - from: "src/app.ts"
      to: "src/middleware/error.middleware.ts"
      via: "global error handler registered after routes"
      pattern: "errorHandler"
    - from: "src/routes/health.routes.ts"
      to: "src/routes/health.schema.ts"
      via: "imports route-owned healthProbeSchema per D-09"
      pattern: "healthProbeSchema"
    - from: "src/routes/health.routes.ts"
      to: "src/middleware/validation.middleware.ts"
      via: "POST /health/probe validates body"
      pattern: "validate"
    - from: "src/middleware/validation.middleware.ts"
      to: "src/utils/errors.ts"
      via: "ValidationError or canonical envelope"
      pattern: "VALIDATION_ERROR"
---

<objective>
Complete the foundation error and validation contracts, then run the full phase verification gate.

Purpose: Ensure all invalid input and server errors have consistent, non-leaky API responses before later phases add auth and task endpoints.
Output: Error class hierarchy, global error handler, Zod validation middleware, route-owned health probe schema, and unit tests.
</objective>

## Phase Goal

**As a** task API maintainer, **I want to** start and validate a secure Express and Prisma foundation, **so that** later authentication and task slices can build on a working API stack.

<execution_context>
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/workflows/work-task-issue.md
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@.planning/phases/01-project-foundation/01-PATTERNS.md
@.planning/phases/01-project-foundation/01-04-SUMMARY.md
@AGENTS.md

<interfaces>
Existing contracts from prior plans:
- `src/app.ts` exports `app`; this plan must ensure `errorHandler` is registered after route mounting.
- `src/routes/health.routes.ts` already exposes POST /health/probe; this plan must add Zod body validation by importing the route-owned schema from `src/routes/health.schema.ts` without changing the response contract for valid emails.
- Error envelope must be `{ error: { code: string, message: string, details?: any } }` per D-04.
</interfaces>
</context>

<must_haves>
Truths: AppError subclasses serialize to the canonical envelope; global error handler maps AppError status codes and production unknown errors safely; Zod middleware validates request body/query/params; POST /health/probe invalid bodies return canonical 400 errors; full tests pass.
Artifacts: src/utils/errors.ts, src/middleware/error.middleware.ts, src/middleware/validation.middleware.ts, src/routes/health.schema.ts, tests/unit/errors.test.ts, tests/unit/error-handler.test.ts, tests/unit/validation.test.ts.
Key links: app uses errorHandler after routes; health route imports its co-located healthProbeSchema per D-09 and uses validate(healthProbeSchema); validation failures and custom errors share the same envelope.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement canonical error handling</name>
  <files>src/app.ts</files>
  <files>src/utils/errors.ts</files>
  <files>src/middleware/error.middleware.ts</files>
  <files>tests/unit/errors.test.ts</files>
  <files>tests/unit/error-handler.test.ts</files>
  <read_first>src/utils/errors.ts</read_first>
  <read_first>src/app.ts</read_first>
  <read_first>src/middleware/error.middleware.ts</read_first>
  <read_first>tests/unit/errors.test.ts</read_first>
  <read_first>tests/unit/error-handler.test.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only src/app.ts, src/utils/errors.ts, src/middleware/error.middleware.ts, tests/unit/errors.test.ts, and tests/unit/error-handler.test.ts.
    Forbidden: src/routes/*, src/middleware/validation.middleware.ts, prisma/*.
    Out of scope: Zod validation middleware and route schemas.
  </boundaries>
  <behavior>
    Expected tests: AppError serializes `{ error: { code, message, details? } }`; NotFoundError maps to 404; ValidationError maps to 400; ForbiddenError maps to 403; ConflictError maps to 409; unknown production errors return code INTERNAL_ERROR and do not include stack or original message details.
  </behavior>
  <action>Create tests first for the D-04 and D-05 envelope and subclass status codes, then implement AppError, NotFoundError, ValidationError, ForbiddenError, and ConflictError in src/utils/errors.ts. Create tests for the global Express error handler, then implement errorHandler in src/middleware/error.middleware.ts so AppError instances return their status and JSON envelope, unknown errors return HTTP 500 with code INTERNAL_ERROR, and production unknown-error responses omit stack traces and internal error messages. Update src/app.ts to register errorHandler after all route mounting and after any 404 handler so Express v5 async errors reach the canonical formatter. Keep Express v5 native async propagation per D-07 and do not add express-async-errors.</action>
  <acceptance_criteria>
    - tests/unit/errors.test.ts covers AppError and all required subclasses.
    - tests/unit/error-handler.test.ts covers AppError handling and production unknown-error handling.
    - Unknown production errors return code INTERNAL_ERROR without stack, details, or original error message.
    - src/app.ts registers errorHandler after routes.
    - No express-async-errors package or import is introduced.
  </acceptance_criteria>
  <verify><automated>rtk npx vitest run tests/unit/errors.test.ts tests/unit/error-handler.test.ts tests/smoke/server.test.ts</automated></verify>
  <done>Custom errors and the global error handler return canonical, non-leaky JSON error responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Zod validation envelope</name>
  <files>src/middleware/validation.middleware.ts</files>
  <files>src/routes/health.schema.ts</files>
  <files>src/routes/health.routes.ts</files>
  <files>tests/unit/validation.test.ts</files>
  <read_first>src/middleware/validation.middleware.ts</read_first>
  <read_first>src/routes/health.schema.ts</read_first>
  <read_first>src/routes/health.routes.ts</read_first>
  <read_first>tests/unit/validation.test.ts</read_first>
  <read_first>src/utils/errors.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only src/middleware/validation.middleware.ts, src/routes/health.schema.ts, src/routes/health.routes.ts, and tests/unit/validation.test.ts.
    Forbidden: src/app.ts, src/utils/errors.ts, src/middleware/error.middleware.ts, prisma/*.
    Out of scope: shared schema barrels, authentication schemas, and task CRUD schemas.
  </boundaries>
  <behavior>
    Expected tests: invalid POST /health/probe body returns HTTP 400 with error.code VALIDATION_ERROR, a string message, and details; valid email still returns HTTP 201 with id and email; middleware can validate body, query, and params targets.
  </behavior>
  <action>Create validation tests first, then implement `validate(schema, target)` in src/middleware/validation.middleware.ts using Zod safeParse for body, query, and params. On validation failure, return HTTP 400 with `{ error: { code: "VALIDATION_ERROR", message: "Request validation failed", details } }` where details comes from Zod flatten output. On success, replace req[target] with parsed data and call next(). Create src/routes/health.schema.ts exporting `healthProbeSchema` with an email string so the schema is co-located with the health route per D-09. Update src/routes/health.routes.ts so POST /health/probe imports that route-owned schema and uses validate(healthProbeSchema) before the controller action per D-08, D-09, and D-10.</action>
  <acceptance_criteria>
    - tests/unit/validation.test.ts covers invalid and valid POST /health/probe requests.
    - validation.middleware.ts supports target values body, query, and params.
    - Validation failures return code VALIDATION_ERROR and the canonical envelope.
    - healthProbeSchema is exported only from src/routes/health.schema.ts.
    - Valid POST /health/probe behavior from Plan 03 remains unchanged.
  </acceptance_criteria>
  <verify><automated>rtk npx vitest run tests/unit/validation.test.ts tests/smoke/server.test.ts</automated></verify>
  <done>Zod validation middleware returns consistent 400 errors and protects POST /health/probe input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client to validation middleware | Untrusted request body, query, and params enter route handling. |
| application errors to HTTP response | Internal exceptions are converted into public API JSON. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/middleware/error.middleware.ts | mitigate | Task 1 returns canonical INTERNAL_ERROR without stack/details for production unknown errors. |
| T-01-02 | Information Disclosure | src/app.ts | mitigate | Plan 04 applied helmet() and tests expected headers. |
| T-01-03 | Elevation of Privilege | src/app.ts CORS | mitigate | Plan 04 applied CORS allowlist and rejection tests. |
| T-01-04 | Denial of Service | src/config/index.ts | mitigate | Plan 04 enforced startup config validation including JWT_SECRET length >= 64. |
| T-01-05 | Tampering | Prisma migrations | mitigate | Plan 03 blocking migration task applied schema before DB verification. |
| T-01-06 | Information Disclosure | src/middleware/validation.middleware.ts | mitigate | Task 2 returns the canonical Zod validation envelope without raw internals. |
| T-01-SC | Tampering | npm installs | mitigate | Depends on Plan 01 package legitimacy checkpoint approval. |
</threat_model>

<verification>
Run `rtk npx prisma migrate deploy && rtk npx prisma generate`, then `rtk npx vitest run --reporter=verbose`, then `rtk npx tsc --noEmit --pretty false`.
</verification>

<success_criteria>
SETUP-01 is complete when the full suite passes and the API foundation has canonical errors, production-safe unknown-error handling, Zod validation, security headers, CORS allowlist, validated startup config, Prisma migrations, health DB read/write, and graceful shutdown.
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-05-SUMMARY.md` when done.
</output>
