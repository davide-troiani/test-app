---
phase: 01-project-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .env.example
  - src/config/index.ts
  - tests/unit/config-validation.test.ts
  - src/app.ts
  - src/middleware/logger.middleware.ts
  - tests/unit/helmet-headers.test.ts
  - tests/unit/cors.test.ts
autonomous: true
requirements:
  - SETUP-01
must_haves:
  truths:
    - "Startup fails fast when required environment variables are missing or unsafe."
    - "JWT_SECRET shorter than 64 characters is rejected before the server starts."
    - "Helmet security headers are present on API responses."
    - "Production CORS rejects unlisted origins and accepts configured origins."
  artifacts:
    - path: "src/config/index.ts"
      provides: "Validated runtime configuration"
    - path: "src/middleware/logger.middleware.ts"
      provides: "Morgan request logger without request bodies"
    - path: "tests/unit/config-validation.test.ts"
      provides: "Config validation behavior tests"
    - path: "tests/unit/helmet-headers.test.ts"
      provides: "Helmet header tests"
    - path: "tests/unit/cors.test.ts"
      provides: "CORS allowlist tests"
  key_links:
    - from: "src/app.ts"
      to: "src/config/index.ts"
      via: "CORS middleware reads validated allowed origins"
      pattern: "CORS_ALLOWED_ORIGINS"
    - from: "src/app.ts"
      to: "src/middleware/logger.middleware.ts"
      via: "middleware chain"
      pattern: "requestLogger"
---

<objective>
Add startup configuration validation and HTTP security middleware to the working skeleton.

Purpose: Mitigate unsafe startup, missing security headers, permissive production CORS, and sensitive request logging risks.
Output: Validated config module, logger middleware, Helmet/CORS app wiring, and unit tests.
</objective>

## Phase Goal

**As a** task API maintainer, **I want to** start and validate a secure Express and Prisma foundation, **so that** later authentication and task slices can build on a working API stack.

<execution_context>
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/workflows/work-task-issue.md
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@.planning/phases/01-project-foundation/01-PATTERNS.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
@AGENTS.md

<interfaces>
Existing contracts from Plan 02:
- `src/app.ts` exports `app`.
- `src/server.ts` should consume `config.PORT` after this plan.
- `src/models/index.ts` exports `prisma`.
- `/health` routes must continue to work after Helmet, CORS, JSON parsing, and logging middleware are added.
</interfaces>
</context>

<must_haves>
Truths: invalid environment variables stop startup; security headers are set; production CORS uses an explicit allowlist; request logging does not include request bodies.
Artifacts: src/config/index.ts, src/middleware/logger.middleware.ts, tests/unit/config-validation.test.ts, tests/unit/helmet-headers.test.ts, tests/unit/cors.test.ts.
Key links: app imports config for CORS allowlist; server imports config for PORT; app uses requestLogger after JSON/security middleware and before routes.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate startup configuration</name>
  <files>.env.example</files>
  <files>src/config/index.ts</files>
  <files>tests/unit/config-validation.test.ts</files>
  <read_first>.env.example</read_first>
  <read_first>src/config/index.ts</read_first>
  <read_first>tests/unit/config-validation.test.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only .env.example, src/config/index.ts, and tests/unit/config-validation.test.ts.
    Forbidden: src/app.ts, src/server.ts, src/middleware/* except listed files, prisma/*.
    Out of scope: Helmet, CORS, routes, and error middleware.
  </boundaries>
  <behavior>
    Expected tests: missing DATABASE_URL fails; missing JWT_SECRET fails; JWT_SECRET shorter than 64 characters fails; invalid PORT fails; valid DATABASE_URL, JWT_SECRET, PORT, NODE_ENV, and CORS_ALLOWED_ORIGINS produce a typed config object.
  </behavior>
  <action>Create tests first for config validation, then implement src/config/index.ts using dotenv/config and a pure `loadConfig(env: NodeJS.ProcessEnv)` function plus exported `config = loadConfig(process.env)`. The config object must include DATABASE_URL, JWT_SECRET, PORT, NODE_ENV, and CORS_ALLOWED_ORIGINS as a string array parsed from a comma-separated env var. Enforce D-15 by requiring JWT_SECRET length >= 64 characters and fail startup with clear configuration error text. Update .env.example to keep the same variables and a 64+ character JWT_SECRET example.</action>
  <acceptance_criteria>
    - tests/unit/config-validation.test.ts covers missing DATABASE_URL, missing JWT_SECRET, short JWT_SECRET, invalid PORT, and valid config.
    - src/config/index.ts exports loadConfig and config.
    - loadConfig returns CORS_ALLOWED_ORIGINS as an array with whitespace trimmed and empty entries removed.
    - JWT_SECRET shorter than 64 characters throws before server startup.
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/config-validation.test.ts</automated></verify>
  <done>Startup configuration validation enforces required env vars, valid PORT, and JWT_SECRET length >= 64.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Helmet CORS and logger</name>
  <files>src/app.ts</files>
  <files>src/middleware/logger.middleware.ts</files>
  <files>tests/unit/helmet-headers.test.ts</files>
  <files>tests/unit/cors.test.ts</files>
  <read_first>src/app.ts</read_first>
  <read_first>src/middleware/logger.middleware.ts</read_first>
  <read_first>tests/unit/helmet-headers.test.ts</read_first>
  <read_first>tests/unit/cors.test.ts</read_first>
  <read_first>src/config/index.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only src/app.ts, src/middleware/logger.middleware.ts, tests/unit/helmet-headers.test.ts, and tests/unit/cors.test.ts.
    Forbidden: src/config/index.ts, prisma/*, src/routes/*, src/controllers/*.
    Out of scope: validation middleware and global error formatting.
  </boundaries>
  <behavior>
    Expected tests: GET /health includes Helmet headers such as x-dns-prefetch-control and x-frame-options; production CORS rejects an Origin not in CORS_ALLOWED_ORIGINS; configured origins receive an Access-Control-Allow-Origin response; request logger is exported without logging request bodies.
  </behavior>
  <action>Create tests first for Helmet headers and CORS allowlist behavior, then update src/app.ts to place helmet() before routes, configure cors() from config.CORS_ALLOWED_ORIGINS with production rejection for unlisted origins, keep express.json(), mount requestLogger from src/middleware/logger.middleware.ts, and preserve /health behavior. Implement requestLogger with morgan using a method/path/status/response-time/user-agent format that does not include request bodies. This implements D-16, D-17, and the research security domain.</action>
  <acceptance_criteria>
    - tests/unit/helmet-headers.test.ts asserts expected Helmet headers on GET /health.
    - tests/unit/cors.test.ts asserts unlisted production origins are rejected and listed origins are allowed.
    - src/app.ts uses helmet() before CORS, JSON parsing, logger, and routes.
    - src/middleware/logger.middleware.ts does not log request bodies.
  </acceptance_criteria>
  <verify><automated>npx vitest run tests/unit/helmet-headers.test.ts tests/unit/cors.test.ts tests/smoke/server.test.ts</automated></verify>
  <done>Express responses include security headers, production CORS is allowlist-driven, and request logging excludes bodies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment to process | Untrusted process environment controls startup behavior and CORS policy. |
| browser origin to API | Origin headers influence whether cross-origin responses are allowed. |
| HTTP request to logs | Request metadata crosses into logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | unknown error responses | mitigate | Plan 04 handles production INTERNAL_ERROR without stack/details. |
| T-01-02 | Information Disclosure | src/app.ts | mitigate | Task 2 applies helmet() and tests expected headers. |
| T-01-03 | Elevation of Privilege | src/app.ts CORS | mitigate | Task 2 configures CORS_ALLOWED_ORIGINS and tests rejection of unlisted production origins. |
| T-01-04 | Denial of Service | src/config/index.ts | mitigate | Task 1 validates env vars at startup including JWT_SECRET length >= 64. |
| T-01-05 | Tampering | database schema | mitigate | Plan 02 completed blocking migration/push before this plan. |
| T-01-06 | Information Disclosure | validation errors | mitigate | Plan 04 creates canonical validation envelope. |
| T-01-SC | Tampering | npm installs | mitigate | Depends on Plan 01 package legitimacy checkpoint approval. |
</threat_model>

<verification>
Run `npx vitest run tests/unit/config-validation.test.ts tests/unit/helmet-headers.test.ts tests/unit/cors.test.ts tests/smoke/server.test.ts`, then `npx tsc --noEmit --pretty false`.
</verification>

<success_criteria>
SETUP-01 has fail-fast environment validation, JWT_SECRET length enforcement, Helmet security headers, allowlist-based production CORS, and safe HTTP request logging.
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-03-SUMMARY.md` when done.
</output>
