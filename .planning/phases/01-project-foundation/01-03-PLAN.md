---
phase: 01-project-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/*/migration.sql
  - src/models/index.ts
  - src/app.ts
  - src/server.ts
  - src/routes/index.ts
  - src/routes/health.routes.ts
  - src/controllers/health.controller.ts
autonomous: true
requirements:
  - SETUP-01
user_setup:
  - service: postgres
    why: "The blocking Prisma migration task must apply the schema to a real local database."
    env_vars:
      - name: DATABASE_URL
        source: "Local PostgreSQL database URL"
must_haves:
  truths:
    - "PostgreSQL connects through Prisma with an initial migration for users, tasks, and comments."
    - "GET /health performs a real database read and returns status ok when the DB is reachable."
    - "POST /health/probe performs a real HTTP-triggered database write through Prisma."
    - "The server starts on the configured port and closes Prisma connections during shutdown."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Initial User, Task, and Comment schema with indexes and soft-delete columns"
    - path: "prisma/migrations/*/migration.sql"
      provides: "Initial Prisma migration"
    - path: "src/models/index.ts"
      provides: "Shared Prisma client"
    - path: "src/app.ts"
      provides: "Express app and route mounting"
    - path: "src/server.ts"
      provides: "HTTP server entry and graceful shutdown"
    - path: "src/routes/health.routes.ts"
      provides: "Health and DB write probe routes"
  key_links:
    - from: "src/routes/health.routes.ts"
      to: "src/models/index.ts"
      via: "Prisma client import"
      pattern: "prisma"
    - from: "src/server.ts"
      to: "src/models/index.ts"
      via: "graceful shutdown disconnect"
      pattern: "\\$disconnect"
    - from: "prisma/schema.prisma"
      to: "prisma/migrations/*/migration.sql"
      via: "rtk npx prisma migrate dev --name init"
      pattern: "CREATE TABLE"
---

<objective>
Implement the walking skeleton: a migrated Prisma schema, a real DB read/write path, and a startable Express server.

Purpose: Prove the API stack works end to end before security and validation hardening layers are added.
Output: Prisma schema and migration, Prisma client module, Express app/server, health routes, and health controller.
</objective>

## Phase Goal

**As a** task API maintainer, **I want to** start and validate a secure Express and Prisma foundation, **so that** later authentication and task slices can build on a working API stack.

<execution_context>
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/workflows/work-task-issue.md
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@.planning/phases/01-project-foundation/01-PATTERNS.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
@AGENTS.md

<interfaces>
Planned contracts from Plan 02 tests:
- `src/app.ts` exports `app` for supertest.
- `src/server.ts` exports lifecycle helpers for graceful shutdown tests.
- `src/models/index.ts` exports `prisma` as a PrismaClient instance.
- `GET /health` returns HTTP 200 JSON with `status: "ok"` when Prisma can execute `SELECT 1`.
- `POST /health/probe` accepts JSON `{ "email": string }`, writes a probe user row through Prisma, and returns HTTP 201 JSON containing `id` and `email`.
</interfaces>
</context>

<must_haves>
Truths: migrations exist and apply; Prisma can connect; GET /health reads from the database; POST /health/probe writes through Prisma; server startup and shutdown work.
Artifacts: prisma/schema.prisma, prisma/migrations/*/migration.sql, src/models/index.ts, src/app.ts, src/server.ts, src/routes/index.ts, src/routes/health.routes.ts, src/controllers/health.controller.ts.
Key links: routes use Prisma client; server disconnects Prisma on SIGTERM/SIGINT; migration SQL matches schema.prisma.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Define Prisma schema and client</name>
  <files>prisma/schema.prisma</files>
  <files>src/models/index.ts</files>
  <read_first>prisma/schema.prisma</read_first>
  <read_first>src/models/index.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-PATTERNS.md</read_first>
  <boundaries>
    Allowed: write only prisma/schema.prisma and src/models/index.ts.
    Forbidden: prisma/migrations/*, src/app.ts, src/routes/*, src/controllers/*, tests/*.
    Out of scope: endpoint implementation and migration creation.
  </boundaries>
  <action>Create prisma/schema.prisma per D-11 through D-14 with PostgreSQL datasource, Prisma client generator, User, Task, and Comment models, uuid string primary keys, User.email unique, Task.status default "TODO", timestamps, deletedAt soft-delete fields, relations, and indexes on email, ownerId, assigneeId, taskId, authorId, status, and deletedAt where applicable. Create src/models/index.ts exporting a singleton PrismaClient named prisma with development query/error logging and production error-only logging.</action>
  <acceptance_criteria>
    - schema.prisma contains model User, model Task, and model Comment.
    - schema.prisma contains deletedAt on all three models.
    - schema.prisma contains @@index entries for ownerId, assigneeId, taskId, authorId, status, and deletedAt.
    - src/models/index.ts exports const prisma from @prisma/client.
  </acceptance_criteria>
  <verify><automated>rtk npx prisma validate</automated></verify>
  <done>Prisma schema and shared Prisma client exist with the required tables, relations, indexes, and soft-delete fields.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply initial migration</name>
  <files>prisma/migrations/*/migration.sql</files>
  <read_first>prisma/schema.prisma</read_first>
  <read_first>prisma/migrations/*/migration.sql</read_first>
  <read_first>.planning/phases/01-project-foundation/01-VALIDATION.md</read_first>
  <boundaries>
    Allowed: write only prisma/migrations/*/migration.sql through Prisma migrate.
    Forbidden: prisma/schema.prisma, src/*, tests/*, package.json.
    Out of scope: manual SQL edits not generated from the current Prisma schema.
  </boundaries>
  <action>After all schema file modifications are complete, run `rtk npx prisma migrate dev --name init` against the configured DATABASE_URL to create and apply the initial migration, then run `rtk npx prisma generate`. If Prisma migrate dev refuses to run non-interactively, stop and report that migration creation requires an interactive local database session; after the migration exists, use `rtk npx prisma migrate deploy` for non-interactive verification. This task mitigates T-01-05 and must complete before any DB-dependent verification.</action>
  <acceptance_criteria>
    - A prisma/migrations directory exists with an init migration.sql generated from schema.prisma.
    - migration.sql creates User, Task, and Comment tables with foreign keys and required indexes.
    - `rtk npx prisma generate` completes after migration creation.
    - `rtk npx prisma migrate deploy` succeeds against the configured database after the migration exists.
  </acceptance_criteria>
  <verify><automated>rtk npx prisma migrate deploy && rtk npx prisma generate</automated></verify>
  <done>The initial Prisma migration has been generated, applied, and Prisma Client has been generated before verification continues.</done>
</task>

<task type="auto">
  <name>Task 3: Build health HTTP DB slice</name>
  <files>src/app.ts</files>
  <files>src/server.ts</files>
  <files>src/routes/index.ts</files>
  <files>src/routes/health.routes.ts</files>
  <files>src/controllers/health.controller.ts</files>
  <read_first>src/app.ts</read_first>
  <read_first>src/server.ts</read_first>
  <read_first>src/routes/index.ts</read_first>
  <read_first>src/routes/health.routes.ts</read_first>
  <read_first>src/controllers/health.controller.ts</read_first>
  <read_first>tests/smoke/server.test.ts</read_first>
  <read_first>tests/smoke/shutdown.test.ts</read_first>
  <read_first>tests/integration/db-connect.test.ts</read_first>
  <read_first>.planning/phases/01-project-foundation/01-PATTERNS.md</read_first>
  <boundaries>
    Allowed: write only src/app.ts, src/server.ts, src/routes/index.ts, src/routes/health.routes.ts, and src/controllers/health.controller.ts.
    Forbidden: prisma/*, src/middleware/*, src/config/*, src/utils/*, tests/*.
    Out of scope: CORS allowlist, Helmet assertions, Zod validation, custom error hierarchy.
  </boundaries>
  <action>Create an Express v5 app that exports `app`, mounts an index router, parses JSON, and exposes health routes. Create a server entry that falls back only to process.env.PORT during this task, listens on the configured port, handles SIGTERM and SIGINT by closing the HTTP server and awaiting prisma.$disconnect(), and exports the server lifecycle helpers required by tests/smoke/shutdown.test.ts. Implement GET /health through health.controller.ts using prisma.$queryRaw SELECT 1 and returning `{ status: "ok", timestamp }`. Implement POST /health/probe through Prisma by accepting an email string from the request body, deleting any prior probe row with that email, creating a User with that email and a non-auth probe password marker, and returning HTTP 201 with `{ id, email }`. This implements D-01, D-02, D-07, D-11, and D-12 without adding authentication behavior.</action>
  <acceptance_criteria>
    - src/app.ts exports `app` and mounts /health.
    - src/server.ts closes the HTTP server and calls prisma.$disconnect() on SIGTERM and SIGINT.
    - GET /health executes a Prisma database read before returning 200.
    - POST /health/probe creates a User row through Prisma and returns 201 with id and email.
    - No express-async-errors dependency or wrapper is introduced.
  </acceptance_criteria>
  <verify><automated>rtk npx vitest run tests/smoke/server.test.ts tests/smoke/shutdown.test.ts tests/integration/db-connect.test.ts tests/integration/migrations.test.ts</automated></verify>
  <done>The walking skeleton has a startable Express server, a migrated Prisma schema, a real DB read, and a real HTTP-triggered DB write.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client to Express routes | Request data enters the API through /health and /health/probe. |
| Express route to Prisma/PostgreSQL | Route handlers execute database reads and writes. |
| Prisma schema to database | Migration SQL changes persistent schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | src/app.ts route errors | mitigate | Plan 05 adds global error handler returning INTERNAL_ERROR without stack/details for unknown production errors. |
| T-01-02 | Information Disclosure | src/app.ts middleware chain | mitigate | Plan 04 adds helmet() before routes and tests expected headers. |
| T-01-03 | Elevation of Privilege | src/app.ts CORS | mitigate | Plan 04 adds environment-driven allowlist and production rejection tests. |
| T-01-04 | Denial of Service | src/server.ts startup | mitigate | Plan 04 replaces fallback env reads with validated config including JWT_SECRET length >= 64. |
| T-01-05 | Tampering | prisma/schema.prisma and migrations | mitigate | Task 2 is the blocking schema migration/push task before DB verification. |
| T-01-06 | Information Disclosure | POST /health/probe invalid input | mitigate | Plan 05 adds Zod middleware returning `{ error: { code, message, details? } }`. |
| T-01-SC | Tampering | npm installs | mitigate | Depends on Plan 01 package legitimacy checkpoint approval before dependencies exist. |
</threat_model>

<verification>
Run `rtk npx prisma validate`, then the blocking `rtk npx prisma migrate deploy && rtk npx prisma generate`, then `rtk npx vitest run tests/smoke/server.test.ts tests/smoke/shutdown.test.ts tests/integration/db-connect.test.ts tests/integration/migrations.test.ts`.
</verification>

<success_criteria>
SETUP-01 has a migrated PostgreSQL schema, a shared Prisma client, a startable Express server, graceful shutdown, and health endpoints proving one DB read and one DB write through HTTP.
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-03-SUMMARY.md` when done.
</output>
