---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - .env.example
autonomous: false
requirements:
  - SETUP-01
user_setup:
  - service: postgres
    why: "Prisma migrations and integration tests require a reachable PostgreSQL database."
    env_vars:
      - name: DATABASE_URL
        source: "Local PostgreSQL database URL, for example postgresql://user:password@localhost:5432/task_api"
      - name: JWT_SECRET
        source: "Local secret string with at least 64 characters"
must_haves:
  truths:
    - "Executor cannot install npm dependencies until the flagged package legitimacy checkpoint is approved."
    - "The project has TypeScript, Vitest, and npm scripts for the API foundation."
    - "The next Wave 0 plan can create executable tests before production implementation."
  artifacts:
    - path: "package.json"
      provides: "Node package metadata, scripts, and dependency declarations"
    - path: "vitest.config.ts"
      provides: "Vitest configuration for unit, smoke, and integration tests"
  key_links:
    - from: "package.json"
      to: "vitest.config.ts"
      via: "npm test script runs Vitest"
      pattern: "vitest run"
---

<objective>
Create the dependency and configuration starting point for Phase 1 without installing unverified packages.

Purpose: Establish the package, TypeScript, Vitest, and environment scaffold that Wave 0 tests and later implementation plans depend on.
Output: Package scaffold, TypeScript/Vitest configuration, and environment example.
</objective>

## Phase Goal

**As a** task API maintainer, **I want to** start and validate a secure Express and Prisma foundation, **so that** later authentication and task slices can build on a working API stack.

<execution_context>
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/workflows/work-task-issue.md
@/Users/davide/repos/get-tasks-done-demo-app/.codex/get-tasks-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-RESEARCH.md
@.planning/phases/01-project-foundation/01-VALIDATION.md
@.planning/phases/01-project-foundation/01-PATTERNS.md
@AGENTS.md
</context>

<must_haves>
Truths: package legitimacy is checked before dependency installation; npm scripts exist for dev, build, test, Prisma migration, and Prisma generate; Vitest is configured for the Wave 0 tests created by Plan 02.
Artifacts: package.json, package-lock.json, tsconfig.json, vitest.config.ts, .env.example.
Key links: npm test script runs Vitest; vitest.config.ts points to tests/setup.ts and tests/teardown.ts that Plan 02 creates; .env.example exposes DATABASE_URL, JWT_SECRET, PORT, NODE_ENV, and CORS_ALLOWED_ORIGINS.
</must_haves>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy</name>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <boundaries>
    Allowed: read npm registry pages or package metadata only.
    Forbidden: package.json, package-lock.json, node_modules/*, source files.
    Out of scope: installing or upgrading packages before approval.
  </boundaries>
  <action>Before any install, verify the npm packages flagged [ASSUMED] in research: express, @prisma/client, zod, helmet, cors, morgan, dotenv, prisma, typescript, tsx, vitest, supertest, @types/express, @types/node, @types/cors, and @types/morgan. Confirm each package name matches the intended registry package and has an established source or npm listing per the Package Legitimacy Audit.</action>
  <acceptance_criteria>
    - Each listed package has been checked on npmjs.com/package or equivalent npm metadata.
    - No package is renamed, substituted, or installed during this checkpoint.
    - Execution resumes only after the user approves the package list.
  </acceptance_criteria>
  <verify><automated>MISSING - human package legitimacy approval is required before install commands can run.</automated></verify>
  <done>User explicitly approves installing the listed npm packages or names the packages to remove.</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript package scaffold</name>
  <files>package.json</files>
  <files>package-lock.json</files>
  <files>tsconfig.json</files>
  <files>vitest.config.ts</files>
  <files>.env.example</files>
  <read_first>package.json</read_first>
  <read_first>tsconfig.json</read_first>
  <read_first>vitest.config.ts</read_first>
  <read_first>.env.example</read_first>
  <read_first>.planning/phases/01-project-foundation/01-CONTEXT.md</read_first>
  <read_first>.planning/phases/01-project-foundation/01-RESEARCH.md</read_first>
  <read_first>AGENTS.md</read_first>
  <boundaries>
    Allowed: write only package.json, package-lock.json, tsconfig.json, vitest.config.ts, and .env.example.
    Forbidden: src/*, tests/*, prisma/*, node_modules/*.
    Out of scope: application source, Prisma schema, routes, middleware, and test files.
  </boundaries>
  <action>After Task 1 approval, install the exact Phase 1 dependency set from research using `rtk npm install`: runtime dependencies express@^5.0.0, @prisma/client@^6.0.0, zod@^3.0.0, helmet@^8.0.0, cors@^2.0.0, morgan@^1.0.0, dotenv@^16.0.0; dev dependencies prisma@^6.0.0, typescript, tsx@^4.0.0, vitest, supertest, @types/express@^5.0.0, @types/node, @types/cors, and @types/morgan. Create package scripts named dev, build, start, test, test:verbose, db:migrate, db:deploy, and db:generate. Configure TypeScript for Node ESM output and Vitest for tests/**/*.test.ts with tests/setup.ts and tests/teardown.ts. Populate .env.example with DATABASE_URL, JWT_SECRET with a 64+ character example value, PORT, NODE_ENV, and CORS_ALLOWED_ORIGINS per D-03, D-15, D-17, and D-18.</action>
  <acceptance_criteria>
    - package.json contains "type": "module" and scripts dev, build, start, test, test:verbose, db:migrate, db:deploy, and db:generate.
    - package-lock.json is generated by npm for the approved packages only.
    - tsconfig.json compiles src/**/*.ts and tests/**/*.ts with strict TypeScript enabled.
    - vitest.config.ts includes setupFiles pointing at tests/setup.ts and global teardown pointing at tests/teardown.ts.
    - .env.example documents DATABASE_URL, JWT_SECRET, PORT, NODE_ENV, and CORS_ALLOWED_ORIGINS.
  </acceptance_criteria>
  <verify><automated>rtk npx tsc --noEmit --pretty false</automated></verify>
  <done>The project has install-verified dependencies, TypeScript configuration, Vitest configuration, npm scripts, and environment examples.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry to local project | Package metadata and install artifacts cross into trusted source control. |
| test configuration to future tests | Vitest setup paths point to files that Plan 02 creates before implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | error tests | mitigate | Plan 02 creates production unknown-error tests for canonical INTERNAL_ERROR without stack/details. |
| T-01-02 | Information Disclosure | app security headers | mitigate | Plan 02 creates helmet header tests; Plan 04 implements helmet(). |
| T-01-03 | Elevation of Privilege | CORS middleware | mitigate | Plan 02 creates CORS allowlist tests; Plan 04 implements CORS. |
| T-01-04 | Denial of Service | config/test setup | mitigate | This plan exposes JWT_SECRET in .env.example; later config task validates length >= 64 at startup. |
| T-01-05 | Tampering | Prisma migrations | mitigate | Plan 02 creates migration verification tests; Plan 03 includes the blocking migration/push task before verification. |
| T-01-06 | Information Disclosure | validation tests | mitigate | Plan 02 creates Zod validation middleware tests for the canonical error envelope. |
| T-01-SC | Tampering | npm installs | mitigate | Blocking human package legitimacy checkpoint is required before npm install because research marked packages [ASSUMED]. |
</threat_model>

<verification>
Run `rtk npx tsc --noEmit --pretty false` after the scaffold task. Plan 02 creates all Wave 0 test files before implementation begins.
</verification>

<success_criteria>
SETUP-01 has an approved dependency base and TypeScript/Vitest scaffold ready for Wave 0 test contracts.
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-01-SUMMARY.md` when done.
</output>
